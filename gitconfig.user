[alias]
  aa = add --all
  amend = commit --amend
  ap = add --patch
  ci = commit --verbose
  co = checkout
  di = diff
  dc = diff --cached
  ff = merge --ff-only
  pf = push --force-with-lease
  st = status
  noff = merge --no-ff
  fa = fetch --all
  pom = push origin master
  ds = diff --stat=160,120
  dh1 = diff HEAD~1

  # Divergence (commits we added and commits remote added)
  div = divergence

  # Goodness (summary of diff lines added/removed/total)
  gn = goodness
  gnc = goodness --cached

  # Fancy logging.
  #   h = head
  #   hp = head with patch
  #   r = recent commits, only current branch
  #   ra = recent commits, all reachable refs
  #   l = all commits, only current branch
  #   la = all commits, all reachable refs
  #   b = all branches
  #   bs = all branches, sorted by last commit date
  head = !git r -1
  h = !git head
  hp = "!. ~/.githelpers.sh && show_git_head"
  r = !GIT_NO_PAGER=1 git l -30
  ra = !git r --all
  l = "!. ~/.githelpers.sh && pretty_git_log"
  la = !git l --all
  b = "!. ~/.githelpers.sh && pretty_git_branch"
  bs = "!. ~/.githelpers.sh && pretty_git_branch_sorted"

[color]
  ui = auto

[mergetool]
  keepBackup = false

# vi:syntax=gitconfig
