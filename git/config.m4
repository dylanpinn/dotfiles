[alias]
	# Add
	aa = add --all
	ap = add --patch

	# Checkout
	co = checkout

	# Commit
	amend = commit --amend
	ci = commit --verbose

	# Diff
	di = diff
	dc = diff --cached

	# Push
	pf = push --force-with-lease

	# Status
	st = status
[user]
	name = NAME
	email = EMAIL
